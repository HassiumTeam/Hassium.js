{
    use TestsStats from "tests_stats.has";

    func main () {

        [a, b, c, ] = [ "c", 2, "DDD", "a", ];
        f = TestsStats ();
        println (f);
        println (c);
    }

    main ();
}
