{
    import "tests_stats.has";

    func main () {
        o = { a: "A", b: 2, c: "C", };
        [a, b, c, ] = o;
        println (a);
        println (b);
        println (c);
    }

    main ();
}
