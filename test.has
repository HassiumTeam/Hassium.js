{
    func main () {
        o = { one: null, two: "to", };
        println (o ["one"]);
        this.test (1, 2);
        println (this.test (3, 4));
    }

    func test (a, b) {
        println ("a + b = " + (a + b));
    }

    main ();
}
