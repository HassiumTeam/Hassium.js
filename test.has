{
    func main () {
        a = true
        this.assert(a, "Boolean is true");
    }

    func assert (c, test) {
        if (c) {
            println ("Passed test: " + test)
        } else {
            println ("Failed test: " + test);
        }
    }

    main ()
}
