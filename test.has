{
    use TestsStats from "tests_stats.has";

    func main () {

        [a, b, c, ] = [ "c", 2, "DDD", "a", ];
        f = io.File("a");
        println (f instanceof io.File);
        println (f);
        println (c);
    }

    main ();
}
